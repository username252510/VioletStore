# VioletStore

nvm i just found out that violetstore will never work even after all that of replacing backloop.dev with localhost.direct. this is because of server sided changes by apple which broke pancakestore snd wafflestore and lioterally any other fork as well.

so this whole project was basically a waste of time but ill keep the repo up in case someone does find a workaround 
